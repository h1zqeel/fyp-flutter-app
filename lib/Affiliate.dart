import 'package:flutter/material.dart';

class Affiliate extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Affiliate'),
        backgroundColor: Colors.blue,
      ),
      body: Container(
        child: Text('Affiliate page'),
      ),
    );;
  }
}
